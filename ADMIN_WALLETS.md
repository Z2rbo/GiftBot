# 💼 Просмотр кошельков пользователей (Админ)

## ✅ Что добавлено:

1. **Таблица `wallets` в базе данных** — хранит все подключенные кошельки
2. **Автоматическое сохранение** — кошельки сохраняются при подключении в веб-аппе
3. **Админ-команды** для просмотра кошельков

---

## 📋 Админ-команды:

### 1. `/wallets` — Все кошельки всех пользователей

Показывает список всех подключенных кошельков:
- 💎 TON кошельки
- 🦊 Metamask (ETH) кошельки

**Пример вывода:**
```
💼 Подключенные кошельки (15)

💎 TON
👤 @username (ID: 123456789)
📍 EQD...abc123
🕒 2026-01-31

🦊 ETH
👤 @username2 (ID: 987654321)
📍 0x1234...5678
🕒 2026-01-30
```

---

### 2. `/userwallets <user_id>` — Кошельки конкретного пользователя

Показывает все кошельки одного пользователя.

**Использование:**
```
/userwallets 123456789
```

**Пример вывода:**
```
💼 Кошельки @username (ID: 123456789)

💎 TON
📍 EQD1234567890abcdef...
🕒 Подключен: 2026-01-31
🔄 Использован: 2026-01-31

🦊 ETH
📍 0x1234567890abcdef...
🕒 Подключен: 2026-01-30
🔄 Использован: 2026-01-31
```

---

## 🔍 Как найти user_id пользователя:

1. Попроси пользователя написать боту `/start`
2. В логах бота (Railway → Logs) будет виден user_id
3. Или используй `/wallets` чтобы увидеть все user_id с кошельками

---

## 📊 Где хранятся данные:

- **База данных:** `bot_database.db` → таблица `wallets`
- **Поля:**
  - `user_id` — ID пользователя Telegram
  - `wallet_type` — тип кошелька (`ton` или `eth`)
  - `wallet_address` — адрес кошелька
  - `connected_at` — когда подключен
  - `last_used` — когда последний раз использован

---

## ⚙️ Как это работает:

1. Пользователь открывает веб-апп в Telegram
2. Подключает TON или Metamask кошелёк
3. Веб-апп автоматически отправляет адрес на сервер через `tg.sendData()`
4. Бот сохраняет адрес в базу данных
5. Админ может посмотреть через команды `/wallets` или `/userwallets`

---

## 🔐 Требования:

- Команды доступны только **админу** (ID указан в `ADMIN_ID` в `.env`)
- По умолчанию: `ADMIN_ID=5637106500`

---

## 📝 Примеры использования:

**Посмотреть все кошельки:**
```
/wallets
```

**Посмотреть кошельки пользователя:**
```
/userwallets 123456789
```

**Найти пользователя по адресу кошелька:**
1. Используй `/wallets` чтобы увидеть все адреса
2. Найди нужный адрес в списке
3. Посмотри user_id рядом с адресом

---

## ✅ После деплоя:

1. Перезапусти бота (Railway автоматически перезапустит)
2. Попроси пользователя подключить кошелёк в веб-аппе
3. Используй `/wallets` чтобы проверить, что адрес сохранился
